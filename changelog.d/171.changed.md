Promoted the HMM from an `phylo.opt` fitting fixture to a first-class
`phylo.sim.hmm` generator that retains the hidden state path alongside the
emitted observations, on the footing the tree simulator already has.
