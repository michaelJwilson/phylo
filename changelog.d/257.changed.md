`CLAUDE.md` gains a **Where the speedup actually is** section: eight decision rules for what a port should do, each derived from a measurement already recorded in `STATUS.md`, and three Python performance references in the routing table. `DEV.md` gains a **Profiling a Hot Path** procedure and states what each instrument cannot say.
