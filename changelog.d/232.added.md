`phylo.qa.likelihood_footprint` measures the memory a simulation and a
likelihood evaluation hold across `ROADMAP.md` §1.2's declared scale and
projects the declared maximum against the 16 GB requirement, closing the
`O(n x L x k)` row `STATUS.md` carried as unmeasured (#232).
