Changelog fragments in `changelog.d/`, folded into `CHANGELOG.md` at release
by `python -m infra.changelog --assemble`. One file per change means two pull
requests never edit the same file, so the changelog cannot conflict on merge.
CI validates fragment names and requires one unless a pull request is
labelled `skip-changelog`.
