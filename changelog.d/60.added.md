Added `phylo.sim.newick`: topology counting (`count_topologies`), Newick string validation (`validate_newick`), and state-labelled Newick serialization (`to_newick`), now the package's single source of Newick functionality.
