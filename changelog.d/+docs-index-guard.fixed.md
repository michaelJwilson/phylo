`docs/source/index.rst` was missing five modules — `phylo.sim.graph`, `phylo.sim.potts`, `phylo.sim.hmm`, `phylo.scripts` and `phylo.scripts.run_phylo` — the third drift of an invariant `docs/CLAUDE.md` states and that `sphinx-build -W` structurally cannot catch, since it fails on a broken entry and never on an absent one. All five are added, and a millisecond test now enforces the invariant in both directions.
