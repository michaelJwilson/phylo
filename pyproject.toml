[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "phylo"
version = "0.1.0"
requires-python = ">=3.12"

[tool.maturin]
python-source = "python"
module-name = "phylo._phylo"
